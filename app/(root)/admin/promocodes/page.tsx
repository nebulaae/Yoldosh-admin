import { Promocodes } from "@/components/pages/admin/Promocodes";

const Page = () => {
  return (
    <section className="w-full p-8">
      <Promocodes />
    </section>
  );
};

export default Page;
